# Public parser fixtures

These Site Manager documents are public test data. Passwords, API keys, grants, account identifiers and file paths are synthetic examples, not application credentials. Some hostnames identify public services or the public Rebex demo server; parser tests do not connect to them. Keep real exported Site Manager files outside this directory.
